"""NeXifyAI Agent Layer — API-First Sub-Agent Architecture.
Central orchestrator + specialized sub-agents.
Model-agnostic: currently uses GPT-5.2 via Emergent LLM Key.
Swap to DeepSeek or other models by changing with_model() call."""
